# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)，
版本号唯一来源是 `transit/__init__.py` 的 `__version__`。

## [1.2.0] — 2026-09-20

修一个会串数据的严重问题，并把术语库质量与校对体验往前推了一步。

### 修复

- **切换工程会串数据（严重）**：翻译完 A 再载入 B 做分析，术语库/风格圣经可能
  仍是 A 的内容、或变成两者混杂。两个独立成因都已修掉：
  - **后端**：任务在跑完后才读 `st.paths` 决定写哪 —— 若期间用户载入了 B，
    A 的术语库就被写进 B 的文件，同时内存里 glossary 是 A 的、data 是 B 的。
    现在任务**开头就快照工程身份**（hash + 各输出路径），写回前校验；
    工程变了就丢弃本次结果并明确报错，绝不写文件。
  - **前端**：术语库页的编辑缓存 `gvEditCache` 只在首次渲染时初始化，
    换文件后**继续显示上一个工程的词条**，一按保存就把 A 的词条写进 B。
    现在轮询时跟踪工程 hash，一旦变化就作废术语库/角色表缓存、修正页回到第 1 页，
    并立即重绘当前页签。
  - 另外：**有任务在跑时禁止切换文件**（返回 409 并说明原因），
    这是防呆；上面两道才是兜底。
- **术语库混入非术语**：模型会把整句塞进「特殊用语/口癖」，例如
  「指定したステータス項目を、ステータス画面と装備画面に表示しないようにします。」，
  也会把句型占位（`〜してください`）当成一个词条。现在三重把关：
  1. 提示词明确禁止收句子/带谓语的短语，并给出反例与长度上限
  2. 入库前按「句末标点 / 句中标点 / 谓语结尾 / 含波浪线 / 超长」判定并拒绝
  3. **用全文校验**：剔除全文里根本没出现的条目（模型幻觉），以及
     「只作为更长词条片段出现」的条目 —— 如已有「バイドーラ」时单收的「バイド」、
     已有「フレイラ」「フレイガ」时单收的「フレイ」。
     判据是该词在全文中的出现次数减去包含它的更长词的出现次数后是否 ≤0，
     即它从未独立出现过。这类碎片会让翻译时一条文本同时命中两个词条、收到矛盾指令。
     被剔除的条目记在 glossary.json 的 `term_pruned` 里以便追溯
- **单条重译要等十几秒**：不是「思考」开着，而是提示词浪费 ——
  实测单条重译发出的 4,747 字符里**只有 22 字符是待译文本**，
  其中**角色表一项就占 59%**（30 个角色 2,817 字符），而那一行根本没出现任何角色名。
  现在单条重译走**精简提示词**：角色表按本行/邻行的相关性裁剪（最多 8 人，
  并保留主角团兜底）、规则精简、`max_tokens` 收紧到 2048。
  实测提示词从 4,821 降到 2,086 字符（省 57%）。
  界面上也会显示已等待秒数，不再让人以为卡死
- **保存译文后表格不刷新**：`保存并下一条` 只在内存里改了数据，背后的表格没有重绘，
  必须切走再切回页签才看得到。现在保存/重译后**就地重绘该行**

### 改进

- **译法分歧可直接点选**：原来只把分歧列出来让你自己去词表里改。
  现在每个分歧直接给出候选按钮（带被提出的次数），点一下就采用并立即保存生效
- **角色表「性别」列被裁掉**：列宽 68px 减掉单元格 10px 内边距只剩 48px，
  而原生下拉框的固有宽度是「最长选项 + 箭头」，装不下就只显示左半侧。
  现在列宽加到 88px、该单元格去掉大内边距、下拉框走紧凑样式，
  表格改用固定布局保证列宽被遵守

## [1.1.1] — 2026-09-20

修两个影响日常使用的问题。

### 修复

- **关掉浏览器后程序还在后台跑，只能用任务管理器结束**：打包版是窗口模式
  （刻意不弹黑色控制台），关掉标签页并不会结束进程。现在：
  - 界面右上角新增 **「退出程序」** 按钮，点了立即结束后台进程
  - **无人连接时自动退出**：以浏览器请求为心跳（页面每 1.5 秒轮询），
    关掉标签页后默认 **10 分钟** 自动退出；可在「设置 → 程序行为」调整，
    填 `0` 表示不自动退出。**有任务在跑时不会退出**
  - 失去连接时页面会明确提示「程序可能已退出，请重新启动 TransIt.exe」，
    而不是一串看不懂的报错；已翻译的进度都在磁盘上，不会丢
- **设置页没跟上新的采样配置**：界面上仍是旧的「分析采样数」（对应已废弃的
  `sample_size`），所以那个输入框**改了不生效**、显示也恒定不变。现在改为
  **采样比例（%）/ 下限 / 上限** 三个字段，并在下方实时预览
  「本文件待译 N 条 → 实际采样 M 条」

### 内部

- 新增 `POST /api/shutdown`（受与其它接口相同的 Host/Origin 防护）
- 看门狗以「最后一次 HTTP 请求」为心跳，仅在任务空闲且超时后退出；
  界面设置的正数不得小于 30 秒（避免页面还没打开就退出），
  命令行 `--exit-when-idle` 不受此限，便于测试

## [1.1.0] — 2026-09-10

分析质量与译文校对体验的一次大改。

### 分析（世界观 / 角色 / 术语）

- **采样量改为按比例**：原来是固定条数（`sample_size`），现在按待译条数取比例，
  并受上下限约束。默认 `sample_ratio=0.10`、`sample_min=200`、`sample_max=1000`：
  3600 条 → 采 360 条；500 条 → 取到下限 200 条；2 万条 → 封顶 1000 条。
  旧配置里的 `sample_size` 会被忽略并给出提示（不再有"改了没生效"的困惑）
- **采样策略改为窗口采样**：原来是在全文里随机抓单句（且长文本全取导致顺序被打乱）；
  现在把全文切成若干段、每段取一段连续文本。样本**保持原文顺序**并覆盖剧本的
  开头/中段/结尾，世界观分析因此能看到连贯对白而非零散单句
- **世界观分析从 1 次请求扩为 3 阶段**，且产出结构化：
  1. **风格圣经**：世界观、游戏上下文（类型/舞台/剧情/玩家视角/基调/文风）、
     **命名方针**（音译或意译、用字偏好、是否保留敬称）、**语气指南**
     （旁白/对白/菜单/成人场景各自的语气要求）、翻译注意
  2. **角色表**（全新）：每个角色的性别、身份、性格、说话风格、第一人称自称、
     被别人怎么称呼、注意事项。这是对白翻译最关键的上下文——日语不标性别，
     中文却必须选"他/她"，自称与称呼方式直接决定语气
  3. **术语提取**：把角色表喂给每一块，避免同一角色在不同块被译成不同写法
- **角色表注入翻译提示词**：译名、性别、语气、自称、称呼关系会随每次翻译请求下发
- **译法分歧不再被默默覆盖**：原来同一原文在不同术语块给出不同译法时，后块直接
  覆盖前块（等于随机丢掉先前的）；现在保留先出现的译法，并把分歧（含出现次数）
  记录下来，在术语库页面列出，由你裁决

### 界面

- 修正页新增 **「排除『原样保留』」复选框**（默认开启）：ID/数字/代码这类本来
  就不需要翻译的条目不再刷屏
- 修正页新增 **条目分类与全局统计**：待译/已有译文/原样保留各多少，其中还有多少未翻译
- 修正页新增 **单条重译**：对某一句直接让 AI 重译，可附带"本次修改要求"
  （如"太书面了，改口语化一点"），旧译文会作为参考一起交给模型。
  不必为了改一句而重跑上千条
- 修正页新增 **保存并下一条**：Ctrl+Enter 连续校对，不用反复开合弹窗
  （Alt+Enter 重译；在"修改要求"框里按 Enter 直接重译）
- 术语库页改为**风格圣经可视化**：世界观、命名方针、语气指南可编辑；
  角色表以表格呈现且**关键字段可直接编辑**（性别是下拉框）；译法分歧单独列块

### 修复

- **非中文 Windows 上命令行会崩**：stdout 被重定向（管道/文件/CI）时 Python 按系统
  ANSI 代码页编码，英文系统是 cp1252，编码不了中文 —— `TransIt-CLI.exe --help`
  这类带中文的输出会直接抛 `UnicodeEncodeError` 退出。现在两个入口启动时都会调用
  `transit.enable_utf8_stdio()` 把标准流切到 UTF-8。中文系统是 cp936 能编码中文，
  所以这个坑只在非中文 Windows 上暴露；另外打包后的 exe 会忽略 `PYTHONIOENCODING`
  （PyInstaller 设置了 `Py_IgnoreEnvironmentFlag`），因此必须在代码里改。
  `build.ps1` 新增了强制 cp1252 的回归防线
- 修正 **「只看未翻译」的语义错误**：原样保留条目永远不会有译文，混在里面会让
  这个筛选失去意义；现在它只显示真正需要翻译且尚未翻译的条目
- 修正 **搜索译文搜不到** 的 bug：原来只比对了原始文件的值，按中文译文搜索永远无结果

### 内部

- **删除 `webui.py` 里重复实现的分析流程**（`run_analysis_with_cb`）。此前 WebUI 与
  CLI 各有一份分析代码，改一处另一处不生效——WebUI 那份还停留在 `samples[:60]`。
  现在统一走 `analyzer.run_analysis`，进度通过 `step_cb` 回调上报
- 术语库保存改为按「客户端实际提交的字段」合并，避免未提交的新字段被归一化默认值清空

## [1.0.0] — 2026-09-10

首个可用版本。免安装便携版，解压即用，无需安装 Python。

### 流水线

- 世界观分析 + 术语建库：采样文本交由模型提取角色名、道具、势力/组织、地名、
  行业术语、特殊口癖，生成可手工编辑的 `*.glossary.json`
- 带行上下文的批量精翻：mtool 按行截断的碎片句可结合前后邻行正确衔接（窗口可调）
- 断点续传：每批落盘，中断后重跑自动跳过已完成条目；多轮收敛补齐漏译
- hash 隔离：按文件内容 SHA-1 区分不同翻译对象，术语库/进度/输出互不覆盖
- 省 token：术语库按批次文本子串过滤，只注入本批相关词条

### 质量与安全

- 防错位合并：键容错匹配（精确 + 归一化）与占位符交叉校验，
  拒绝键值错位——错译比漏译危害更大，漏译会重试，错译会静默进入成品
- 占位符保护：`<SG...>`、`\n`、`\v[n]`、`{数字}` 等控制符原样保留
- 翻译风格可选：R18 两档（explicit / moderate）、名词风格四档
  （auto / kawaii / simple / transliterate）、精修润色轮、自定义提示词
- 本地接口防护：WebUI 只监听 `127.0.0.1`，`/api/*` 校验 `Host` 与 `Origin` 白名单，
  响应带 `X-Frame-Options: DENY` 与 CSP `frame-ancestors 'none'`；
  令牌作为可选加固（默认关闭）
- 重复启动幂等：检测到已运行的实例只打开浏览器指向它，不再起第二个服务

### 界面与分发

- 浏览器图形界面（纯标准库 `http.server`，前端单文件无外链）四个页面：
  工作台 / 术语库编辑 / 译文逐句修正 / 设置
- 命令行入口 `transit_cli.py`，以及开发期备选的 tkinter 界面 `transit_gui.py`
- 零第三方运行依赖
- PyInstaller 单目录便携版：两个 exe 共用一个 `_internal` ——
  `TransIt.exe`（窗口模式 WebUI）与 `TransIt-CLI.exe`（控制台，可脚本化）
- 数据（`config.json` / `output` / `uploads`）全部保存在 exe 同目录，整包可迁移
- `build.ps1` 一键构建 + 冒烟测试 + 打包 ZIP + 生成 SHA256

### 已知限制

- 仅 Windows
- 术语库按单个输入文件隔离；同一游戏被拆成多个文件时，需用 `--glossary` 手工共享
- 术语采样有上限（默认 300 条），晚出场的专有名词可能未被收录
- 占位符不一致目前仅记入日志（warning），不计为失败；翻译后建议人工核查
- 仅支持 mtool 的 `{原文: 译文}` JSON 格式
- 无译文长度约束，长译文在固定宽度文本框内可能溢出

[1.0.0]: https://github.com/687jsassd/TransIt/releases/tag/v1.0.0
